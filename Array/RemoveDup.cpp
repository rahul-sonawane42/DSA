#include <bits/stdc++.h>

using namespace std;

void remdup(int arr[], int n){
    int newarr[n];
    for (int i =0; i< n; i++){
        
    }
}

int main() {
    int n;
    cout<<"Enter number of elements: ";
    cin>>n;
    int arr[n];
    for (int i=0; i<n; i++){
        int num;
        cout<<"Enter element "<< i+1<<": "<<endl;
        cin>>num;
        arr[i] = num;
    }

    remdup(arr,n);
    return 0;
}